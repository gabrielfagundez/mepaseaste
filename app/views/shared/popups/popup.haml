#outPopUp.hide
#inPopUp.hide
  .innerTextPopUp
    %p
      A quien pertenece este destino?
    %input{id: 'nombre_marcador', name: 'nombre_marcador', type: 'text', value: '', class: 'input-type-text', size: '80'}
    %p
      Cuantas personas van al mismo?
    %select{id: 'customDropdown'}
      - for i in 1..10
        %option{value: i}
          = i

    %br
    .botones_pop_up
      .right
        .button{onclick: 'agregarNombre()'}
          Aceptar
      .left
        .button{onclick: 'eliminarUltimoMarcador()'}
          Cancelar