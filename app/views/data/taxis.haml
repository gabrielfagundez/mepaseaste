.row
  .large-12.columns
    .panel.radius
      %h3
        Para la consulta que has realizado, se #{ @query.solution.length == 1 ? 'formó' : 'formaron   ' } #{ @query.solution.length } taxis.
        El costo total del viaje es de $#{ @query.costo_total.to_s }. La mejor distribución de personas y taxis, es la siguiente:
      %br
      %h3
        #formacion_taxis
          - if @query.solution
            - @query.solution.each_with_index do |taxi, index|
              .panel.yellow-background
                .taxi
                  %a
                    .imagen-taxi{ onclick: "calcRoute(#{ index })" }
                  .texto-taxi
                    -#- taxi.each do |ubicacion|
                    -#  .row
                    -#    .small-3.columns
                    -#      = ubicacion
          - else
            Calculando.. Por favor espere.