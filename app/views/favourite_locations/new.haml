.row
  .small-12.columns
    .full-yellow-upper-line
    .panel.radius.text-center
      %h2 Crear una ubicación favorita

    - if @favourite_location.errors.any?
      = content_tag(:div, @favourite_location.errors.full_messages.join('<br>'), class: 'panel radius callout text-center')

    .panel.radius
      = render 'form'

:javascript
  $(function(){
    FavouriteLocations.init(true);
  })